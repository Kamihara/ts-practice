export {};

let numbers: number[] = [1, 2, 3];

let numbers2: Array<number> = [4, 5, 6];

let strings2: Array<string> = ['Tokyo', 'Osaka', 'Fukuoka'];

let strings: string[] = ['JavaScript', 'TypeScript', 'CoffeeScript'];

// let twoDimArray: Array<Array<number>> = [
let twoDimArray: number[][] = [
    [10, 20],
    [30, 50],
];

let hairetsu: (number|boolean|string)[] = [1, false, 'status'];
// let hairetsu: (number|boolean|string)[] = [1, false, 'status', [1, 2, 3]];
